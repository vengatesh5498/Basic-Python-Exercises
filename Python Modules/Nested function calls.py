#These are function calls inside other function calls
#When executed, the inner most function calls are resolved first
#The returned value is used for the outer function
print(round(abs(float(input("Enter a whole positive number: ")))))

